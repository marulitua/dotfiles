[pager]
	diff = delta
	show = delta
	log = delta
	reflog = delta

[interactive]
	diffFilter = delta --color-only

[include]
    path = /media/eins/repos/delta/themes.gitconfig

[delta]
    line-numbers = true
    side-by-side = true
    features = woolly-mammoth
